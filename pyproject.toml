[project]
name = "wadmanager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flet[all]>=0.80.5",
    "loguru>=0.7.3",
    "pypubsub>=4.0.7",
    "toml>=0.10.2",
]
